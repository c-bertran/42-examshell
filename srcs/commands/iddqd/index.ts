import clock from 'modules/clock';
import { command } from '../interface';

export default {
	name: 'status',
	description: 'Print status of current examen',
	exec: (commands, lang, clock: clock) => {
		
	}
} as command;